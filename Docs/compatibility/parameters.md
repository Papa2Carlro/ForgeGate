# Parameter Compatibility Policy

Not "unsupported → silently drop everything". Not fail on every optional parameter. Explicit parameter compatibility policy.

Conceptual classification: RequiredSemantic; OptionalSemantic; Advisory; ProviderSpecific. Exact classification NOT finalized.

# Unsupported Semantic Requirements

If parameter represents required semantic contract route cannot provide, do NOT silently remove. Example: strict structured JSON/schema requirement + route cannot guarantee/support → route ineligible or explicit compatibility error. May participate in routing eligibility.

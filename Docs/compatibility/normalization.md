# Parameter Normalization

If provider supports safe equivalent representation, translate parameter. Example: max_completion_tokens → provider equivalent max_tokens when semantics sufficiently equivalent. Translation = compatibility adjustment, not silent loss.

# Capability Mismatch

First-class route eligibility reason. Examples: requires tools + route lacks tools → INELIGIBLE; requires vision + route lacks vision → INELIGIBLE; required context exceeds route context → INELIGIBLE. Do NOT silently send semantically incompatible request.

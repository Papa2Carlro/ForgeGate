# Model Route Responsibilities

ModelRoute may carry: provider-native model identifier; declared capabilities; observed capabilities; context limit; provider-specific constraints; health; capacity/quota scope; enabled state; priority/quality override; observed reliability; provider quirks. Do NOT duplicate provider-global facts unnecessarily. Exact ownership finalized during implementation.

# User Interaction on Significant Degradation

ASK_USER appropriate when downgrade significant for current task and client supports ask-user capability. Minor route changes (same logical model through another provider) may be automatic. Do NOT assume all OpenAI-compatible clients expose native ask-user tool.

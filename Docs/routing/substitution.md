# Explainable Substitution

When preferred route skipped, retain reason. Conceptual: preferred skipped: CAPABILITY_MISMATCH(tools). Substitution policy: same logical model, different provider → normally automatic; different model, same quality tier → generally automatic per profile; lower quality tier → DEGRADE / ASK_USER per policy; no eligible route → explicit capability/routing failure. Observable in audit/admin.

# Bounded Prompt Injection

No unbounded supervision text. Additions must be minimal, relevant, deduplicated, scoped, removable when no longer needed.

Important for token efficiency, model attention, avoiding conflicting instructions, keeping free-provider usage practical.

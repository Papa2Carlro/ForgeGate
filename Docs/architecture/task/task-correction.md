# Task Manager as Corrective Supervision

Task Manager activates when bad execution behavior is detected.

Example: goal is fix parser bug; agent starts unrelated refactor. Scope-drift signal fires. Supervisor responds conceptually: REPLAN — active goal is X; current work has drifted outside intended scope; update plan and return to goal.

Purpose: automatic correction of the kind a human currently provides manually.

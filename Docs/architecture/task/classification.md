# Task Classification

Adaptive classification. Not mandatory for every request.

Precedence:
1. Explicit task metadata (optional, syntax not finalized).
2. Cheap deterministic/heuristic classification.
3. LLM Task Analyzer only when ambiguous or low confidence.

Simple tasks must stay simple without metadata.

# Adaptive Task Management

Task management intensity depends on: model behavior profile, task complexity, observed drift, current execution behavior, supervision configuration.

Trivial task → minimal context. Stable strong model → lightweight tracking. Weak model / repeated drift → stricter tracking, explicit checkpoints, comparison with goal. Complex implementation → decomposition/milestones. Research/debugging → evolving hypotheses/evidence rather than rigid predeclared plan.

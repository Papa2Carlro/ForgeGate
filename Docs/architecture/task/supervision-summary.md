# Adaptive Supervision Summary

Not one mechanism. Combines: deterministic policy checks; behavior detectors; Task Manager; semantic policy reasoner; task analyzer; adaptive prompt/context injection; workspace coordination; cross-session signals.

Different models/tasks use different subsets and intensities.

# Structured Execution Handoff

Not raw full replay, not one giant master prompt, not LLM summary only.

Preferred: Stable checkpoint + factual events since checkpoint + current verified workspace state + bounded semantic summary → replacement model.

Includes: goal, constraints, effective profile, completed milestones, current hypothesis, verified evidence, changed resources, verification results, pending actions, open questions, policy history, workspace conflicts, last stable point, handoff reason.

No final DTO yet.

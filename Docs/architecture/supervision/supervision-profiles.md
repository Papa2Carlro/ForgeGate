# Configurable Supervision Profiles

Supervision intensity is not globally fixed. Different models need different control levels.

Conceptual profiles:
- weak/unstable agent: strict supervision, frequent semantic checkpoints, high scope-drift sensitivity.
- strong coding model: light supervision, rare checkpoints.

Configuration schema NOT finalized.

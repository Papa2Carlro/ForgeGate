# Workspace Coordination

Distinct concern from session supervision.

Session supervision: "What is this session doing?"
Workspace coordination: "What are other active sessions in the same workspace doing?"

Workspace-level context: recent file mutations, active mutation intent, touched paths, recent policy decisions, overlapping work, foreign dirty changes, active tracks. Queryable by deterministic logic, reasoners, supervisors, audit.

# Hierarchical Supervision Configuration

Effective behavior influenced by multiple scopes (not finalized merge semantics):
Instance defaults → Workspace overrides → Model profile → Virtual routing profile → Session/task overrides.

Key decision: supervision is configurable and contextual, not one fixed global policy.

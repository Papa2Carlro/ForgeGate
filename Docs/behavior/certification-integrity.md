# Certification Integrity

Detect invalid patterns: `cmd | tail/head/grep` then treating `$?` as tested command's exit code; manually printing PASS/exit-code strings; using broad suites as substitutes for specifically requested focused proof.

Evidence must derive from actual command/runtime results.

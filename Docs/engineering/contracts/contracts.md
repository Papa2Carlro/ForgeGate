# Explicit Contracts at External/Replaceable Boundaries

Explicit abstractions around volatile external dependencies: provider transport; secret storage; clock/time for deterministic testing; persistence repositories/query ports; capacity coordination; semantic policy reasoner; external model discovery. Do NOT hide stable internal domain code behind unnecessary interfaces.

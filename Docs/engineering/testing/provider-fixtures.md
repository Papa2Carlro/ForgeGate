# Provider Test Fixtures

Provider integration testing should support deterministic fixtures/synthetic payloads. Examples: provider success response fixtures; normalized failure fixtures; malformed response fixtures; tool-call fixtures; streaming/SSE fixtures; quota/rate-limit fixtures. Provider adapters should not require real external API call for every ordinary CI test.

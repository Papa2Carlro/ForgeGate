# Dependency Injection

ASP.NET Core DI = application composition mechanism. Constructor-injected dependencies. No service locator, no static mutable services, no manual IServiceProvider resolution from business code, no global singleton accessors. Composition root wires concrete implementations to contracts.

# Core Engineering Style

Strong OOP + SOLID. Encapsulation, explicit responsibilities, dependency inversion, interface segregation, composition over hidden coupling, replaceable components, testable boundaries, cohesive classes/modules, explicit contracts.

Avoid procedural god services, large static helpers, hidden global state.

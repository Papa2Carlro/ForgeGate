# Explicit Optionality

If absence has meaningful domain/application semantics, model that semantic state explicitly where appropriate. Examples: Unknown; NotObserved; Unavailable; NotApplicable — should not automatically be encoded as null when distinction matters.

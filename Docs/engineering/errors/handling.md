# Error Handling

Exceptions not normal routing/business-control flow. Expected outcomes use explicit result/state models. Exceptions for unexpected exceptional failures. Provider-specific errors normalized at boundary before higher-level routing consumes them. No raw infrastructure exceptions through application logic.

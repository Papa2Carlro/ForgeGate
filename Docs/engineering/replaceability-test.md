# Architectural Replaceability Test

Mental test: "If this implementation replaced tomorrow, how much unrelated code changes?" Desired: composition/config changes + new implementation, not edits throughout unrelated code. Design heuristic, not mandate to abstract every class.

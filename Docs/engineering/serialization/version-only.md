# Version Only Stable Boundaries

Do NOT version every internal record/class. Versioning required where compatibility matters across time/system boundaries: durable polymorphic activity payloads; external/admin contracts where backward compatibility promised. Internal temporary implementation models may evolve normally.

# Configuration Examples

Bootstrap/static: database connection/bootstrap; server/listen settings; secret-store selection; infrastructure startup defaults; development bootstrap. Runtime operational: providers; model routes; routing profiles; policy settings; admin-editable config. Secrets in SecretStore. Health/cooldowns/current capacity = runtime state.

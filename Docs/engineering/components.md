# Small Cohesive Components

Prefer small cohesive classes with explicit collaboration. Not maximum class count. Avoid one-method-one-class, interface-for-every-implementation without reason, excessive wrapper/decorator chains, unnecessary generic abstractions. Small meaningful blocks, not abstraction for abstraction's sake.

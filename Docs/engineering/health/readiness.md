# Readiness

Readiness = whether ForgeGate is operational enough to serve API/control plane according to configured deployment mode. Potential prerequisites: successful application composition/startup; valid mandatory startup config; required persistence availability; required database schema/migrations; required internal invariants initialized. Exact dependency list finalized during implementation.

# Provider Extensibility

New provider requires: provider-specific adapter/transport; provider-specific normalization; capability/config registration; error mapping. Must NOT require editing routing core with provider-name conditionals. Provider quirks remain local to provider infrastructure boundaries.

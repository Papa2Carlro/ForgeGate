# Deterministic Hard Denies

Even if semantic supervision unavailable, deterministic prohibited operations remain DENY. Example: candidate clearly matches deterministic prohibited operation → DENY without semantic reasoner.

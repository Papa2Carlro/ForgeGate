# Structural Tool Validation

Before policy reasoning: tool exists in exposed tool set; arguments valid enough for declared schema; tool-call structure complete; malformed partial streaming fragments not released as executable calls. Exact validation library/schema mechanics = implementation decision.

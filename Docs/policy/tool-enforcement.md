# Tool-Call Validation and Policy Enforcement

Layered enforcement: structural validation → deterministic policy → suspicion/risk signal → semantic reasoner only when needed → ALLOW / DENY / REPLAN / ASK_USER. Not deterministic-only long-term; not LLM reasoner for every call.

# Admin UI — Operational MVP + Control Plane Direction

NOW: Operational Admin MVP (dashboard, providers, models/routes, routing profiles, policies/supervision, activity/audit).

DIRECTION: Full Control Plane.

Do NOT implement full future control plane in first MVP. Backend/domain boundaries should support evolution without rewriting provider/routing/policy/domain models.

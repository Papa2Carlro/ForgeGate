# Admin UI — Deferred from MVP

Defer: sophisticated policy DSL editor; arbitrary rule graph builder; direct manual TaskExecutionContext editing; checkpoint/handoff editor; full secret rotation lifecycle; automatic supervision self-tuning; distributed cluster controls; advanced planning supervisor config; every low-level internal threshold; full provider marketplace/plugin management UI.

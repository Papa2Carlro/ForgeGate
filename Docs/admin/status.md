# MVP Architectural Status Update

DECIDED: hybrid buffered streaming; capability-based provider abstraction; provider capabilities vs model-route capabilities distinction; declared vs observed capabilities vs runtime state; normalized provider failure taxonomy + preserved raw evidence; retryability and failure scope distinct; layered conservative session resolution; false merge worse than split; durable structured facts in PostgreSQL; configurable/selective raw payload retention; derived projections not canonical facts; secret-store abstraction; persisted credential references; mandatory secret redaction/masking; Operational Admin MVP; direction toward fuller Admin Control Plane.

NOT YET DECIDED / DEFERRED: exact commit threshold; exact buffer policy; exact tool-call streaming assembly; exact provider interface names/shapes; exact capability contracts; health probing schedule; discovery lifecycle; final normalized error enum; final retryability model; final failure-scope model; provider-specific normalization mappings; exact fingerprint algorithm; exact confidence representation; VS Code metadata availability; physical DB schema; event payload schema; retention defaults/expiry; projection rebuild mechanics; production secret-store implementation; rotation workflow; OS keychain/Vault support; exact admin page/component design; exact backend admin API; full control-plane feature set; fine-grained policy DSL.

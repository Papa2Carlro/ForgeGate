# Decision Triggers / Future Discussion Backlog

BEFORE Streaming Pipeline: decide exact commit boundary; tool-call fragment buffering/validation; stream interruption representation.
BEFORE Provider Adapters: finalize minimum provider contract; first capability contracts; provider-specific error normalization boundary.
BEFORE Router: finalize retryability interpretation; capacity/quality selection inputs; provider cooldown handling.
BEFORE Session Resolver: inspect actual VS Code Custom Endpoint request metadata; determine stable conversation identifiers; finalize fallback fingerprint.
BEFORE Persistence: finalize first durable entities/events; payload redaction/retention defaults; projection ownership.
BEFORE Secret Management beyond local MVP: production encryption/key management; rotation lifecycle.
BEFORE Admin UI beyond operational MVP: review actual operational pain points; expand toward full Control Plane.
BEFORE Agent Guard runtime: finalize ActionIntent schema; capability registry design; translation engine architecture; correction loop bounds.

Do NOT resolve deferred decisions during this documentation task.

# MVP Decision Status Update

DECIDED: external MVP /v1/models + /v1/chat/completions; internal canonical protocol-neutral model; hard eligibility → quality tier → operational ranking; composite route health; passive-first health; selective active probes; central in-process Capacity Coordinator; capacity scoped by provider/credential/model route; profile-driven wait/degradation; task-aware quality floor; manual model quality baseline; observed runtime quality without automatic tier reassignment; capability mismatch as first-class ineligibility; explainable substitution; compact RoutingDecision; optional verbose trace; Provider/LogicalModel/ModelRoute separation; three addressing modes; explicit parameter compatibility policy; no silent semantic parameter degradation; provider-local normalization; no global provider-conditional sprawl; layered tool enforcement; deterministic-first policy; semantic reasoning only on suspicious/ambiguous; pre-execution enforcement; policy outcomes; correction loop integration.

NOT YET DECIDED / DEFERRED: exact OpenAI error-body fidelity; optional Chat Completions parameter matrix; Responses API; other OpenAI APIs; exact operational ranking formula; profile wait budgets; degradation thresholds; quality-tier config format; exact health enums; probe cadence; cooldown formulas; exact capacity reservation; token estimation; sliding-window implementation; persistence/restart of temporary counters; distributed coordinator; automatic benchmarking; automatic tier reassignment; benchmark suite; learned quality model; exact parameter classification; compatibility error shape; provider quirk representation; exact risk/suspicion score; semantic escalation threshold; Policy Reasoner model; correction prompt; replan retry count.

# PostgreSQL Persistence Boundary

Durable structured facts + configurable/selective raw payload retention. Not almost nothing; not everything forever.

Durable concepts: ProviderConfig; ModelRoute; RoutingProfile; PolicyConfig; Workspace; Session; ActivityEvent; ProviderAttempt; PolicyDecision; UsageRecord; CompletionAssessment; task/effective state where durability useful.

Physical schema NOT finalized from this list.

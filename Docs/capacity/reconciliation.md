# Capacity Reconciliation

Before provider call: reserve estimated capacity. After completion: reconcile against actual provider usage when available; local estimates otherwise; observed failures (e.g., rate limits). Perfect token prediction NOT required. Not a billing engine.

# Capability Information Sources

Distinguish three sources:

DeclaredCapabilities (provider API / docs / config).
ObservedCapabilities (ForgeGate probes / runtime evidence).
RuntimeState (current operational state, e.g., rate-limited).

Do NOT collapse into single ProviderStatus object. Schema undecided.

# Local development environment variables
# Copy to .env and fill in any needed values.
# NEVER commit .env or any file containing real credentials.

POSTGRES_USER=forgegate
POSTGRES_PASSWORD=forgegate_dev
POSTGRES_DB=forgegate
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Database connection string (used by the API)
FORGEGATE_DATABASE_URL=Host=localhost;Port=5432;Database=forgegate;Username=forgegate;Password=forgegate_dev
